# Build output / generated
.next/
out/
build/
coverage/
playwright-report/
test-results/
next-env.d.ts
pnpm-lock.yaml

# Frozen design inputs — not owned by the app, do not reformat
plans/
prototypes/
.superpowers/

# Helm chart templates: Go-template syntax ({{ ... }}) isn't valid standalone
# YAML, so Prettier's YAML parser cannot parse these files.
helm/keel/templates/
