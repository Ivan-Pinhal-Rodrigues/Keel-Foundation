replicaCount: 2

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

ingress:
  enabled: true
  className: "nginx"
  host: "keel.example.com"

config:
  logLevel: "info"

autoscaling:
  enabled: false
